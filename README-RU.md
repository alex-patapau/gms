# 🎯 GMS Lightbox System

**Современная, Модульная, Мобильная JavaScript Библиотека Лайтбокса**

Полностью расширяемая, плагинная система лайтбоксов, построенная на ES модулях, с поддержкой просмотра PDF, галерей изображений, видео и многого другого. Разработана для бесшовной интеграции на любой сайт без конфликтов имен.

---

## ✨ Возможности

- 🧩 **Модульная Архитектура** - Плагинная система с ядром + модулями функционала
- 📱 **Mobile-First** - Оптимизирована для iOS Safari, Android Chrome, планшетов
- 🎨 **Без Конфликтов** - Все классы/функции используют префикс `gms-`
- ⚡ **На Базе Vite** - Быстрая разработка и оптимизированные продакшн сборки
- 📄 **Поддержка PDF** - Полная интеграция PDF.js с зумом, навигацией, прокруткой
- 🌍 **Мультиязычность** - Автоматическое определение языка браузера (6 языков)
- 🎹 **Навигация с Клавиатуры** - ESC для закрытия, стрелки для навигации
- 👆 **Сенсорные Жесты** - Свайп-навигация на мобильных устройствах
- 📦 **Tree-Shakeable** - Импортируйте только то, что нужно
- 📚 **Хорошо Документирован** - JSDoc комментарии повсюду
- ♿ **Доступный** - ARIA метки, поддержка клавиатуры, управление фокусом

---

## 📦 Установка

### Вариант 1: Режим Разработки (Рекомендуется)

```bash
# Установить зависимости
npm install

# Запустить сервер разработки
npm run dev
```

Откройте `http://localhost:3000` для просмотра демо.

### Вариант 2: Продакшн Сборка

```bash
# Собрать для продакшн
npm run build

# Просмотр продакшн сборки
npm run preview
```

Собранные файлы будут в директории `dist/`.

### Вариант 3: Отладочная Сборка

```bash
# Собрать с исходными картами и console.log
npm run build-debug

# Просмотр отладочной сборки
npm run preview-debug
```

Отладочные файлы будут в директории `dist-debug/` с сохраненными `console.log` и source maps.

### Вариант 4: Прямая Интеграция

Подключите собранные файлы в ваш HTML:

```html
<link rel="stylesheet" href="path/to/dist/assets/gms-main-[hash].css">
<script type="module" src="path/to/dist/assets/gms-main-[hash].js"></script>
```

---

## 🚀 Быстрый Старт

### Базовое Использование

Добавьте атрибут `data-glightbox` или класс `gms-lightbox` к любому элементу:

```html
<!-- Пример PDF -->
<a href="document.pdf" 
   class="gms-lightbox" 
   data-gms-type="pdf"
   data-title="Мой Документ">
  📄 Открыть PDF
</a>

<!-- Или с data-glightbox (совместимость) -->
<a href="document.pdf" 
   data-glightbox 
   data-type="pdf">
  Открыть PDF документ
</a>

<!-- Изображения (В Разработке) -->
<a href="photo.jpg" 
   class="gms-lightbox" 
   data-gms-type="image"
   data-title="Фото заката">
  Посмотреть Фото
</a>

<!-- Видео (В Разработке) -->
<a href="video.mp4" 
   class="gms-lightbox" 
   data-gms-type="video">
  Воспроизвести Видео
</a>
```

### Обязательные Атрибуты

- `class="gms-lightbox"` или `data-glightbox` - Маркирует элемент как триггер лайтбокса
- `data-gms-type` или `data-type` - Тип медиа: `pdf`, `image`, `video`, `gallery`
- `href` - URL медиа-файла

### Опциональные Атрибуты

- `data-title` или `data-gms-title` - Заголовок/описание медиа (если не указан, используется текст ссылки)
- `data-gallery` - Имя группы галереи (для галерей)

---

## 🌍 Мультиязычность

### Автоматическое Определение Языка

GMS Lightbox автоматически определяет язык браузера и показывает интерфейс на соответствующем языке:

```javascript
// Система определяет язык из браузера
navigator.language // например: "ru-RU", "en-US", "uk-UA"

// Использует первую часть до дефиса
language.split('-')[0] // "ru", "en", "uk"
```

### Поддерживаемые Языки (6)

| Язык | Код | Пример интерфейса |
|------|-----|-------------------|
| 🇬🇧 **English** | `en` | Page 1 of 5, Reset, Prev, Next |
| 🇷🇺 **Русский** | `ru` | Страница 1 из 5, Сброс, Назад, Вперёд |
| 🇺🇦 **Українська** | `uk` | Сторінка 1 з 5, Скинути, Назад, Вперед |
| 🇩🇪 **Deutsch** | `de` | Seite 1 von 5, Zurücksetzen, Zurück, Weiter |
| 🇫🇷 **Français** | `fr` | Page 1 sur 5, Réinit, Préc, Suiv |
| 🇪🇸 **Español** | `es` | Página 1 de 5, Restablecer, Anterior, Siguiente |

### Переведенные Элементы

**Индикатор загрузки:**
- 🇬🇧 "Loading PDF..."
- 🇷🇺 "Загрузка PDF..."
- 🇺🇦 "Завантаження PDF..."
- 🇩🇪 "PDF wird geladen..."
- 🇫🇷 "Chargement PDF..."
- 🇪🇸 "Cargando PDF..."

**Навигация по страницам:**
```
[← Назад] Страница 1 из 5 [Вперёд →]
[← Zurück] Seite 1 von 5 [Weiter →]
[← Préc] Page 1 sur 5 [Suiv →]
```

**Управление масштабом:**
```
[-] [100%] [+] [Сброс]
[-] [100%] [+] [Zurücksetzen]
[-] [100%] [+] [Réinit]
```

Подробнее: [LOCALIZATION.md](LOCALIZATION.md)

---

## 🏗️ Архитектура

### Структура Проекта

```
gms/
├── src/
│   ├── core/
│   │   ├── gms-core.js          # Ядро системы лайтбоксов
│   │   └── gms-core.scss        # Базовые стили
│   ├── modules/
│   │   ├── pdf/
│   │   │   ├── gms-pdf.js       # Модуль просмотра PDF
│   │   │   └── gms-pdf.scss     # Стили PDF
│   │   ├── image/
│   │   │   └── gms-image.js     # Просмотр изображений (заглушка)
│   │   ├── video/
│   │   │   └── gms-video.js     # Видеоплеер (заглушка)
│   │   └── gallery/
│   │       └── gms-gallery.js   # Просмотр галерей (заглушка)
│   ├── lib/
│   │   ├── pdf.min.mjs          # Библиотека PDF.js
│   │   └── pdf.worker.min.mjs   # Worker для PDF.js
│   ├── styles/
│   │   └── gms-main.scss        # Главный файл стилей
│   └── gms-main.js              # Точка входа
├── index.html                   # Демо страница
├── vite.config.js               # Конфигурация Vite
├── package.json                 # Метаданные проекта
├── BUILD-MODES.md               # Документация по режимам сборки
└── LOCALIZATION.md              # Документация по локализации
```

### Модульная Система

GMS следует модульной архитектуре:

#### 1. Ядро (`gms-core.js`)

**Обязанности:**
- Управление глобальным оверлеем и контейнером
- Жизненный цикл открытия/закрытия лайтбокса
- Обработка событий клавиатуры (ESC, стрелки)
- Парсинг атрибутов из триггерных элементов
- Определение типа медиа и роутинг
- Делегирование событий для динамических элементов
- Определение мобильных устройств и адаптация
- Предотвращение прокрутки при активном лайтбоксе

**Публичное API:**
```javascript
import { gmsInitCore, gmsRegisterModule, gmsOpenLightbox, gmsCloseLightbox } from './core/gms-core.js';

// Инициализировать систему ядра
gmsInitCore();

// Зарегистрировать пользовательский модуль
gmsRegisterModule('custom-type', renderFunction);

// Программно открыть лайтбокс
gmsOpenLightbox(element);

// Программно закрыть лайтбокс
gmsCloseLightbox();
```

#### 2. Модули Функционала

Каждый модуль обрабатывает определенный тип медиа:

**PDF Модуль (`gms-pdf.js`)** ✅ Полностью Реализован
- Инициализация библиотеки PDF.js
- Настройка Worker для фоновой обработки
- Рендеринг многостраничных PDF на canvas
- Управление масштабом (увеличение/уменьшение/сброс)
- Навигация по страницам (предыдущая/следующая/на основе прокрутки)
- Мобильная оптимизация масштабирования (2x на мобильных)
- Поддержка сенсорных жестов (свайп)
- Кеширование canvas для производительности
- Ленивый рендеринг
- Мультиязычный интерфейс (6 языков)
- Относительные пути для Worker (работает в любых директориях)
- Разделение кода (code splitting) - основной бандл 4.3KB, PDF модуль 456KB

**Модуль Изображений (`gms-image.js`)** 🚧 Заглушка
- Отображение одиночных изображений
- Зум и панорамирование
- Пинч-зум касанием
- Поддержка подписей
- Адаптивное масштабирование

**Видео Модуль (`gms-video.js`)** 🚧 Заглушка
- Воспроизведение HTML5 видео
- Поддержка встраивания YouTube/Vimeo
- Пользовательские элементы управления
- Опции автовоспроизведения

**Модуль Галереи (`gms-gallery.js`)** 🚧 Заглушка
- Просмотр коллекций
- Навигация по миниатюрам
- Поддержка смешанных медиа
- Навигация с клавиатуры/свайпом

---

## 🎨 Стилизация

### Соглашение об Именовании

**Все пользовательские классы используют префикс `gms-`** для избежания конфликтов:

- Ядро: `.gms-lightbox-overlay`, `.gms-lightbox-container`, `.gms-lightbox-close`
- PDF: `.gms-pdf-viewer`, `.gms-pdf-page`, `.gms-pdf-controls`
- Кнопки: `.gms-btn`, `.gms-pdf-nav-btn`, `.gms-pdf-zoom-btn`

### Кастомизация

Переопределите стили в своем CSS:

```css
/* Кастомный фон оверлея */
.gms-lightbox-overlay {
  background: rgba(0, 0, 0, 0.95);
}

/* Кастомные цвета кнопок */
.gms-pdf-nav-btn {
  background-color: #your-color;
}

/* Адаптация под мобильные */
@media (max-width: 768px) {
  .gms-pdf-controls {
    padding: 10px;
  }
}
```

### SCSS Переменные

Редактируйте `src/core/gms-core.scss` для изменения глобальных переменных:

```scss
$gms-primary-color: #4a6ee0;
$gms-overlay-bg: rgba(0, 0, 0, 0.85);
$gms-transition-speed: 300ms;
```

---

## 📱 Поддержка Мобильных Устройств

### Поддерживаемые Платформы

✅ iOS Safari (iOS 12+)  
✅ Android Chrome (Android 6+)  
✅ Mobile Firefox  
✅ Samsung Internet  

### Мобильные Возможности

- **Адаптивное Масштабирование**: Контент адаптируется под viewport (320px - 2048px)
- **Сенсорные Жесты**: Свайп влево/вправо для навигации, тап для закрытия
- **Оптимизированный Рендеринг PDF**: 2x масштаб на мобильных для четкого текста
- **Плавная Прокрутка**: `-webkit-overflow-scrolling: touch`
- **Viewport Meta**: Обрабатывает UI мобильных браузеров (адресная строка)
- **Области Касания**: Минимум 44x44px для кнопок (iOS HIG)
- **Поддержка Альбомной Ориентации**: Специальные адаптации для landscape

---

## 🔨 Конфигурация Сборки

### Режимы Сборки

GMS поддерживает 3 режима сборки:

#### 1. Development (Разработка)
```bash
npm run dev
```
- Hot Module Replacement (HMR)
- Source maps включены
- Console.log сохранены
- Без минификации
- Быстрая пересборка

#### 2. Production (Продакшн)
```bash
npm run build
```
- Полная минификация (Terser)
- Удаление console.log
- Оптимизация бандла
- Хеши в именах файлов
- Разделение кода (code splitting)
- Вывод: `dist/`

#### 3. Debug (Отладка)
```bash
npm run build-debug
```
- Source maps включены
- Console.log сохранены
- Читаемый код (beautify)
- Без mangling имен
- Все комментарии сохранены
- Вывод: `dist-debug/`

Подробнее: [BUILD-MODES.md](BUILD-MODES.md)

### Конфигурация Vite

Редактируйте `vite.config.js` для пользовательских настроек сборки:

```javascript
export default defineConfig({
  build: {
    outDir: isDebug ? 'dist-debug' : 'dist',
    sourcemap: isDev || isDebug,
    rollupOptions: {
      output: {
        entryFileNames: isDebug ? 'assets/gms-main-[hash].js' : 'assets/gms-main-[hash].js',
        assetFileNames: 'assets/gms-main-[hash].[ext]'
      }
    },
    terserOptions: isDebug ? {
      format: { beautify: true, comments: 'all' },
      mangle: false
    } : {
      compress: { drop_console: true }
    }
  }
});
```

### Разделение Кода (Code Splitting)

PDF модуль загружается динамически для оптимизации:

```javascript
// Основной бандл: ~4.3 KB
// PDF модуль: ~456 KB (загружается только при открытии PDF)

// В gms-main.js
const module = await import('./modules/pdf/gms-pdf.js');
```

**Преимущества:**
- Быстрая начальная загрузка страницы
- Модули загружаются по требованию
- Меньший размер основного бандла

### Структура Вывода

После сборки:

```
dist/                           # Продакшн
├── index.html
├── assets/
│   ├── gms-main-[hash].js     # 4.32 KB - основной бандл
│   ├── gms-pdf.min-[hash].js  # 456.44 KB - PDF модуль
│   ├── gms-main-[hash].css    # Стили
│   └── pdf.worker.min.mjs     # 1.1 MB - PDF.js worker

dist-debug/                     # Отладка
├── index.html
├── assets/
│   ├── gms-main-[hash].js     # 6.27 KB + 10.84 KB source map
│   ├── gms-pdf.min-[hash].js  # 772.46 KB + 1.09 MB source map
│   ├── gms-main-[hash].css
│   └── pdf.worker.min.mjs
```

---

## 🔧 Продвинутое Использование

### Добавление Пользовательского Модуля

Создайте новый файл модуля:

```javascript
// src/modules/custom/gms-custom.js
import { gmsRegisterModule } from '../../core/gms-core.js';

async function gmsCustomRender({ url, title, element, container, isMobile }) {
  console.log('[GMS Custom] Рендеринг пользовательского контента');
  
  // Ваша логика рендеринга
  const customElement = document.createElement('div');
  customElement.className = 'gms-custom-viewer';
  customElement.textContent = 'Пользовательский контент: ' + url;
  
  container.appendChild(customElement);
}

// Автоматическая регистрация модуля
(function() {
  gmsRegisterModule('custom', gmsCustomRender);
})();

export { gmsCustomRender };
```

Импортируйте его в `gms-main.js`:

```javascript
import './modules/custom/gms-custom.js';
```

Используйте в HTML:

```html
<a href="#" 
   class="gms-lightbox" 
   data-gms-type="custom" 
   data-url="custom-data">
  Открыть Пользовательский Контент
</a>
```

### Программное Управление

```javascript
import { gmsOpenLightbox, gmsCloseLightbox, gmsGetState } from './core/gms-core.js';

// Открыть лайтбокс программно
const element = document.createElement('a');
element.setAttribute('data-gms-type', 'pdf');
element.setAttribute('href', 'document.pdf');
gmsOpenLightbox(element);

// Закрыть программно
gmsCloseLightbox();

// Получить текущее состояние
const state = gmsGetState();
console.log('Лайтбокс активен:', state.isActive);
console.log('Текущий тип:', state.currentType);
```

---

## 🐛 Устранение Неполадок

### Частые Проблемы

**Лайтбокс не открывается**
- Проверьте консоль на ошибки
- Убедитесь, что атрибут `class="gms-lightbox"` или `data-glightbox` присутствует
- Проверьте, что `data-gms-type` соответствует зарегистрированному модулю
- Проверьте, что `gmsInitCore()` вызван

**PDF не загружается**
- Проверьте, что файлы PDF.js находятся в `/assets/` директории
- Проверьте правильность URL PDF и его доступность
- Ищите CORS ошибки в консоли
- Убедитесь, что PDF файл валидный

**Стили не работают**
- Проверьте, что CSS импортирован: `import './styles/gms-main.scss'`
- Убедитесь, что Vite компилирует SCSS (пакет sass установлен)
- Проверьте конфликты имен CSS классов

**Мобильная версия не работает**
- Тестируйте на реальном устройстве, не только в браузере
- Проверьте наличие viewport meta тега
- Проверьте, что обработчики touch событий прикреплены
- Проверьте iOS-специфичные CSS проблемы

**Worker не найден (404)**
- Убедитесь, что `pdf.worker.min.mjs` скопирован в `dist/assets/`
- Проверьте путь: должен быть `./pdf.worker.min.mjs` (относительный)
- В Bitrix: не используйте абсолютные пути `/assets/`

### Режим Отладки

Включите подробное логирование:

```javascript
// В консоли браузера
localStorage.setItem('gms-debug', 'true');
location.reload();

// Или соберите в debug режиме
npm run build-debug
npm run preview-debug
```

---

## 📚 Справочник API

### Основные Функции

#### `gmsInitCore()`
Инициализирует основную систему лайтбоксов. Вызывайте один раз при готовности DOM.

**Возвращает:** `void`

**Пример:**
```javascript
import { gmsInitCore } from './core/gms-core.js';
gmsInitCore();
```

---

#### `gmsRegisterModule(type, renderFn)`
Регистрирует новый модуль контента.

**Параметры:**
- `type` (string) - Идентификатор типа медиа
- `renderFn` (Function) - Функция рендеринга для этого типа

**Возвращает:** `void`

**Пример:**
```javascript
gmsRegisterModule('pdf', gmsPdfRender);
```

---

#### `gmsOpenLightbox(element)`
Открывает лайтбокс с контентом из элемента.

**Параметры:**
- `element` (HTMLElement) - Триггерный элемент с data атрибутами

**Возвращает:** `void`

---

#### `gmsCloseLightbox()`
Закрывает активный лайтбокс.

**Возвращает:** `void`

---

### PDF Модуль

#### `gmsPdfRender(params)`
Рендерит PDF документ в лайтбоксе.

**Параметры:**
- `params.url` (string) - URL PDF файла
- `params.title` (string) - Заголовок PDF
- `params.element` (HTMLElement) - Триггерный элемент
- `params.container` (HTMLElement) - Контейнер контента
- `params.isMobile` (boolean) - Флаг мобильного устройства

**Возвращает:** `Promise<void>`

---

## 🤝 Участие в Разработке

### Рабочий Процесс

1. Форкните репозиторий
2. Создайте ветку функционала: `git checkout -b feature/my-feature`
3. Внесите изменения
4. Протестируйте на нескольких устройствах/браузерах
5. Сделайте коммит с описательными сообщениями
6. Сделайте push и создайте Pull Request

### Стиль Кода

- Используйте префикс `gms` для всех пользовательских идентификаторов
- Добавляйте JSDoc комментарии ко всем публичным функциям
- Включайте инлайн-комментарии для сложной логики
- Следуйте существующей структуре кода
- Тестируйте на мобильных устройствах

---

## 📄 Лицензия

MIT License - свободно используйте в личных и коммерческих проектах.

---

## 🙏 Благодарности

- **PDF.js** - Библиотека Mozilla для рендеринга PDF
- **Vite** - Быстрый инструмент сборки и dev сервер
- **GLightbox** - Первоначальное вдохновение (не используется в финальной версии)

---

## 📞 Поддержка

- **Проблемы**: [GitHub Issues](https://github.com/alex-patapau/gms/issues)
- **Обсуждения**: [GitHub Discussions](https://github.com/alex-patapau/gms/discussions)
- **Документация**: Этот README + встроенные JSDoc комментарии

---

## 🗺️ Дорожная Карта

- [x] Система ядра лайтбокса
- [x] PDF модуль со всеми функциями
- [x] Мобильная оптимизация
- [x] Сенсорные жесты
- [x] Система сборки Vite с 3 режимами
- [x] Отладочная сборка (build-debug)
- [x] Относительные пути для Worker
- [x] Разделение кода (code splitting)
- [x] Мультиязычность (6 языков)
- [x] Исчерпывающая документация
- [ ] Модуль изображений
- [ ] Видео модуль
- [ ] Модуль галереи
- [ ] TypeScript определения
- [ ] Unit тесты
- [ ] E2E тесты
- [ ] CDN распространение
- [ ] npm пакет

---

## 📖 Дополнительная Документация

- **[BUILD-MODES.md](BUILD-MODES.md)** - Подробное руководство по режимам сборки (dev/production/debug)
- **[LOCALIZATION.md](LOCALIZATION.md)** - Руководство по мультиязычности и добавлению новых языков
- **[GUIDE.md](GUIDE.md)** - Руководство по интеграции для Bitrix и других CMS
- **[SUMMARY.md](SUMMARY.md)** - Краткое описание проекта
- **[VALIDATION.md](VALIDATION.md)** - Чеклист валидации перед релизом

---

**Создано с ❤️ для современной веб-разработки**
